name: Server Tests
on:
  workflow_dispatch:
  push:
    branches:
      - develop
jobs:
  call-workflow-passing-data:
    uses: Sanket322/testing/.github/workflows/server-tests.yml@main
    with: 
      base_ref: ""
      REPO_NAME: ${GITHUB_REPOSITORY#$GITHUB_REPOSITORY_OWNER/}
